# Start Linera locally (without Docker)
# Run this in a separate PowerShell window if you want Linera running locally
# NOTE: This script should be run in WSL, not Windows PowerShell directly

Write-Host "🔗 Starting Linera Network Locally..." -ForegroundColor Cyan
Write-Host ""
Write-Host "⚠️  This script should be run in WSL/Linux, not Windows PowerShell" -ForegroundColor Yellow
Write-Host "   Use: wsl bash start-linera-local.sh" -ForegroundColor Gray
Write-Host ""
Write-Host "Or run in WSL terminal:" -ForegroundColor Cyan
Write-Host "   ./start-linera-local.sh" -ForegroundColor White
Write-Host ""
exit 1

